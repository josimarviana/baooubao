#Arquivo de exemplo para configura��o local. Para rodar o projeto �
# necess�rio criar um arquivo "local.properties" com as configura��es referente ao banco de dados local

DB_URL=jdbc:mariadb://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_DATABASE:baoounao}?createDatabaseIfNotExist=true&serverTimezone=UTC
DB_USER=${DB_USERNAME:root}
DB_PASS=${DB_PASSWORD:123456}